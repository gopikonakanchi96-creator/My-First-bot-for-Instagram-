version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./ai_social_bot:/app/ai_social_bot
      - ./logs:/app/logs
    restart: unless-stopped
